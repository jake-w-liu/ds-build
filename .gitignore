/target
**/*.rs.bk
.DS_Store

# Local DS/relay runtime state and live-test artifacts
/.ds/
/live-audit/
/.lyceum-trash/

# Local upstream-sync scratch (if created under repo)
.upstream-sync/
upstream/reviews/
