[package]
license = "Apache-2.0"
name = "ds-markdown-core"
version = "0.1.0"
edition.workspace = true
description = "Headless markdown analysis sharing DS Build's exact pulldown-cmark config."

[dependencies]
pulldown-cmark = { workspace = true }

[lints]
workspace = true
