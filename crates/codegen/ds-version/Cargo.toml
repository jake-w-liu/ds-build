[package]
license = "Apache-2.0"
name = "ds-version"
version = "0.1.32"
edition.workspace = true
description = "Lockstepped ds CLI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
